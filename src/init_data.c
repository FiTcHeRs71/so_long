
#include "../include/so_long.h"

void	init_data(t_mlx *mlx)
{
	mlx->window.size_x = WIDTH;
	mlx->window.size_y = HEIGHT;
	/*mlx->window.min_width = MIN_WIDTH;
	mlx->window.min_height = MIN_HEIGHT;
	mlx->window.max_width = MAX_WIDTH;
	mlx->window.max_height = MAX_HEIGHT;*/
}